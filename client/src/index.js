import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);

// commented out strict mode for Link to work

//   <React.StrictMode>
//     <App />
//   </React.StrictMode>
// );
